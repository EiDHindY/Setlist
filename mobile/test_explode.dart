import 'package:youtube_explode_dart/youtube_explode_dart.dart';

void main() async {
  final yt = YoutubeExplode();
  
  try {
    print('Methods on SearchClient:');
    // We'll write a quick bash script to list them since reflection is tricky
  } catch (e) {
    print(e);
  }
}
